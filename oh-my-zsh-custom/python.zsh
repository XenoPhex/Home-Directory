# make python 3 the default
export PATH=/usr/local/opt/python/libexec/bin:$PATH

# Apparently this flip-flops for with and without the 3, so add both.
export PATH=/usr/local/opt/python@3/libexec/bin:$PATH
